# docs/

- `pin-mapping.md` — full pin table, timer/peripheral setup, DRV8833 truth table.
- `report.pdf` — the course report. **Not committed.** Export it from
  `Report_Embedded.docx` and drop it here. Before exporting, fix the
  "Interrupts: enabled" line — it was inaccurate for the original submission
  and is only true from v2 onward (Timer0 tick) — and drop any measured-sounding
  figures (>95 %, 4–6 h battery, …) that were not actually measured.
